# App nativa multiplataforma con React Native

v1 se entrega como app nativa iOS y Android con **un codebase React Native**, no PWA. El job “fuente más cercana” + GPS + cámara para aportes justifica nativo; React Native es la elección concreta de stack multiplataforma.
